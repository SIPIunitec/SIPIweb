@model IEnumerable<SIPIweb.Models.tbl_usuarioEstudiante>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.usuarioEstudiante_ingreso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_usuarioEstudianteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.usuarioEstudiante_ingreso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_usuarioEstudianteNavigation.usuario_email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_usuarioEstudiante">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_usuarioEstudiante">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_usuarioEstudiante">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
