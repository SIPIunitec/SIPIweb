@model IEnumerable<SIPIweb.Models.tbl_usuarioRole>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.usuarioRol_nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.usuarioRol_nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_usuarioRoles">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_usuarioRoles">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_usuarioRoles">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
